{
  "name": "unireserva",
  "version": "1.0.0",
  "description": "Implementação inicial do sistema UniReserva com front-end e back-end.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "db:init": "node scripts/initDb.js",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "pg-mem": "^3.0.5",
    "supertest": "^7.1.0"
  }
}
